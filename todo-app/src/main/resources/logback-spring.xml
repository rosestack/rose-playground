<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <!-- Patterns based on https://github.com/spring-projects/spring-boot/blob/v3.0.0/spring-boot-project/spring-boot/src/main/resources/org/springframework/boot/logging/logback/defaults.xml -->
  <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
  <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>
  <!-- 输出 traceId/spanId 到日志，便于排查与校验 MDC 透传 -->
  <property name="CONSOLE_LOG_PATTERN" value="%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint} %clr(%5p) %clr([${PID:- }]){magenta} %clr(---){faint} %clr([%15.15t,%X{traceId:-},%X{spanId:-}]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %m%n"/>
  <property name="FILE_LOG_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} %5p [${PID:- }] --- [%t,%X{traceId:-},%X{spanId:-}] %-40.40logger{39} : %m%n"/>
  <springProperty scope="context" name="appName" source="spring.application.name"/>

  <springProfile name="default">
    <root level="INFO">
      <appender-ref ref="CONSOLE"/>
    </root>
  </springProfile>

  <springProfile name="!default">
    <property name="LOG_FILE" value="logs/${appName}/all.log"/>
    <include resource="org/springframework/boot/logging/logback/file-appender.xml"/>

    <appender name="ASYNC" class="ch.qos.logback.classic.AsyncAppender">
      <queueSize>512</queueSize>
      <appender-ref ref="FILE"/>
    </appender>

    <root level="INFO">
      <appender-ref ref="ASYNC"/>
      <appender-ref ref="CONSOLE"/>
    </root>
  </springProfile>
</configuration>
