# 测试环境配置
spring:
  # 数据源配置
  datasource:
    url: jdbc:mysql://localhost:3306/iot_platform_test?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: password
  
  # Redis配置
  data:
    redis:
      host: localhost
      port: 6379
      password: 
      database: 1
  
  # RabbitMQ配置
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

# 日志配置
logging:
  level:
    io.github.rose.iot: INFO
    org.springframework.security: INFO
    com.baomidou.mybatisplus: INFO
    com.alibaba.druid: INFO

# IoT平台配置
iot:
  # MQTT配置
  mqtt:
    broker-url: tcp://localhost:1883
  
  # 数据存储配置
  data:
    retention-days: 60
    compression-enabled: true
    batch-size: 500